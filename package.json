{
  "name": "wtp-framework-root",
  "private": true,
  "description": "Workspaces root for the WTP framework repo. Never published; it only lets Changesets find the two packages (mcp, cli).",
  "workspaces": [
    "mcp",
    "cli"
  ],
  "scripts": {
    "release": "changeset publish",
    "changeset": "changeset",
    "drift": "node scripts/check-drift.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.0"
  }
}
